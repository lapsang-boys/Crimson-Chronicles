package InitRegions

import Regions
import Constants
import TypeCasting

init
    A0P0.add(rectToIndex(gg_rct_A0P0Spawn))
    A0P0.add(rectToIndex(gg_rct_A0P0Node1))
    A0P0.add(rectToIndex(gg_rct_A0P0Node2))
    A0P0.add(rectToIndex(gg_rct_A0P0Node3))
    A0P0.add(rectToIndex(gg_rct_A0P0End))

    playerArea.add(rectToIndex(gg_rct_A0P0Region))

    for p = 0 to NUMBER_OF_PLAYERS-1
        nodes1.add(A0[p].get(1))
        nodes2.add(A0[p].get(2))
        nodes3.add(A0[p].get(3))
        ends.add(A0[p].get(4))
    