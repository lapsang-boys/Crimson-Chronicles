package Poacher

import Assets
import Damage
import UnitObjEditing

import TowerDefinition
import TowerConstants
import BuilderConstants
import Tooltip
import ID2S

constant DAMAGE_BASE = 3
constant DAMAGE_SIDES_PER_DIE = 6
constant DAMAGE_NUMBER_OF_DIE = 1
constant RANGE = 700
constant ATTACK_SPEED = 0.8
constant GOLD_COST = 10
constant POINT_VALUE = 10

class Poacher extends TowerDefinition
    construct(int id)
        super(id)
        // -------------------------------------
        // ========== Model file etc. ==========
        // -------------------------------------
        setIconGameInterface(Icons.bTNTuskaarGold)
        setModelFile(Units.tuskarRanged)
        setTintingColorRed(255)
        setTintingColorGreen(255)
        setTintingColorBlue(255)
        setUnitSoundSet("Tuskarr")
        setScalingValue(0.85)
        setGroundTexture(ID_FULLMOON_TRIBE_GROUND_TEXTURE)

        // ------------------------------------
        // ========== Attack damage ==========
        // ------------------------------------
        setAcquisitionRange(RANGE.toReal())
        setAttack1Range(RANGE)
        setAttack1CooldownTime(ATTACK_SPEED)
        setAttack1DamageBase(DAMAGE_BASE)
        setAttack1DamageSidesperDie(DAMAGE_SIDES_PER_DIE)
        setAttack1DamageNumberofDice(DAMAGE_NUMBER_OF_DIE)

        // ------------------------------------
        // ========== Spells ==================
        // ------------------------------------
        setNormalAbilities(ID2S(ID_SELL_TOWER))

        // ------------------------------------------------
        // ========== Attack modelfile speed etc ==========
        // ------------------------------------------------
        setAttack1ProjectileArt(Abilities.tuskarSpear)
        setAttack1ProjectileHomingEnabled(true)
        setAttack1ProjectileSpeed(1200)
        setAttack1WeaponType(WeaponType.Missile)

        // ----------------------------------------------------------
        // ========== Stats (cost, upgrades, requirements) ==========
        // ----------------------------------------------------------
        setGoldCost(GOLD_COST)
        setPointValue(POINT_VALUE)
        setUpgradesTo("")
        setUpgradesUsed("")

        // ---------------------------------
        // ========== Description ==========
        // ---------------------------------
        setButtonPositionX(3)
        setButtonPositionY(0)
        setHotkey("R")
        setName("Poacher")
        setTooltipBasic(tooltipBuild("R", "Poacher"))
        setTooltipExtended(tooltipTowerExtended("High range tower with the ability to place traps.",
            damageBoundary(DAMAGE_BASE, DAMAGE_SIDES_PER_DIE, DAMAGE_NUMBER_OF_DIE),
            ATTACK_SPEED,
            RANGE,
            "",
            "Poacher, hunter, trapper."))

@compiletime function tower()
    new Poacher(ID_TOWER_POACHER)